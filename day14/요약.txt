https://objectaid.com/

https://objectaid.com/install-objectaid
스텝 따라서 설치


패키지에서 컨트롤 N 한다음에 object 타이핑 

자바는 부모가 하나밖에올수없다
그래서 단일상속이다. c++은 다중상속이 됨

오버라이된 메소드는 try 로 예외처리해야함  익셉션 불가 

부모것을 상속받을떄 기본생성자는 자동으로 생성된다
안보여도 묵시적으로 생성됨 
public BadBank() {

super();  // super() 기본생성자는 안써줘도 무조건 호출됨 
}   

// 명시적으로 써주어야하는 경우에는 다른처리보다 먼저 기본생성자가 호출되도록
해야한다. 
// super() 부모클래스의 생성자를 호출할때는 메서드 처리부분 맨위에 위치시켜야함

//파라미터 있는 생성자가 하나도 없는경우 
// 기본생성자가 자동으로 생성된다 (묵시적)
//파라미터 있는 생성자를 만들어줄때는 기본생성자도 습관적으로 
//함께 명시적으로(눈에보이게)  만들어주세요.! 
// 

jar 파일 만드는법
export 한다음에  java-jar file 프로젝트별로 묶을수있음 .
그러면 jar 파일이 만들어지는데
그걸 다른곳에서 빌드패스 해서 라이브러리 가져올수있음.